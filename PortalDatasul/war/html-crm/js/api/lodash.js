/*globals index, define, angular, CRMURL, CRMUtil, CRMRestService, callback*/

define([
	'index'
], function (index) {

	'use strict';

	var lodash;
    
    lodash = function ($totvsresource) {

        var specificResources, factory;
    };
});