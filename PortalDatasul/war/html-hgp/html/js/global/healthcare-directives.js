define(['index' /*,
        '/healthcare/html/templates/workarea/workareaController.js'*/
        ], function(index) {       
        
        /*
    index.register.directive('healthcareWorkarea', function () {
        return {
            restrict: 'E',
            replace:true,
            transclude:true,
            templateUrl: '/healthcare/html/templates/workarea/workarea_template.html',
            link: function (scope, element, attrs) {                    
                    var pageWrapper = $('div.page-wrapper');
                    if (pageWrapper && pageWrapper.length > 0) {
                            window.Ps = window.Ps || require('perfect-scrollbar');
                            Ps.initialize(pageWrapper[0]);
                    }
            }
        };
    });   */ 
    
});